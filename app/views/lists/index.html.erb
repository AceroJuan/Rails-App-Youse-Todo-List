<div class="list_task_img">
  <div class="list_task_container">
    <h2>Pending Lists</h2>
    <% @lists.each do |list| %>
      <%= link_to(list.list_name, user_list_path(current_user, list.id), class: "btn1") %>
    <% end %>
    <div class="actions"><br /><br/>
      <%= link_to "New List",  new_user_list_path(current_user.id), class: "btn2" %> <br/>
    </div>
  </div>
</div>  